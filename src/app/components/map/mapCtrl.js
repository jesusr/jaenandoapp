(function() {
  'use strict';
  var mapCtrl = function($scope) {
    function init() {}
    angular.extend($scope, {});
    init();
  };

  angular.module('app.map').controller('mapCtrl', mapCtrl);
})();

